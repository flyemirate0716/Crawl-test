import sp from './sp.json';
import en from './en.json';
import ca from './cat.json';

export const dictionaryList = { en, sp, ca };

export const languageOptions = {
    en: 'English',
    sp: 'Española',
    ca: 'Català'
};