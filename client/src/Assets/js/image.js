import { PUBLIC_PATH } from './constant';

export const Logo = `${PUBLIC_PATH}/images/logo.png`;
export const header_logo = `${PUBLIC_PATH}/images/main_logo.png`;
export const Profile_photo = `${PUBLIC_PATH}/images/user.jpeg`;